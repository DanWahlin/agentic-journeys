import SwiftUI

// Placeholder screen from the starter project. Phase 2 replaces it with TodoListView.
struct StarterView: View {
    var body: some View {
        Text("SmartTodo")
            .accessibilityIdentifier("starterTitle")
    }
}
