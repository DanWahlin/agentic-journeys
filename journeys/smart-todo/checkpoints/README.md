# SmartTodo checkpoints

The finished code of each phase, from a validation run. `setup/setup.mjs --start-at <phase>` applies the checkpoints for the phases before it, so you can start the journey at any phase. See [Checkpoints](../PLAN.md#checkpoints) for the layout. The setup script doesn't copy this folder into your workspace.
